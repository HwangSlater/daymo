# 우리의 여행 UI 프로토타입

Expo/React Native로 만든 모바일 UI 우선 프로토타입입니다. 아직 인증과 서버는 연결하지 않았으며, 실제 여행 원문을 바탕으로 한 로컬 시드 데이터만 사용합니다.

## 실행

```bash
cd mobile
npm install
npm run ios
# 또는 npm run android / npm run web
```

포함한 흐름: 홈 → 여행 상세 → 일정/장소/음식/준비/메모/사진 탭, 준비물 체크, 하단 탭 전환, 기본 검색 UI.
