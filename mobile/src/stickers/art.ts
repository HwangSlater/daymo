/**
 * 추억 카드 스티커 그림. 모양 자료만 있다.
 *
 * react-native 를 가져오지 않는다. 앱은 `StickerArt.tsx` 가 이 자료를
 * react-native-svg 로 옮기고, 시험(`node --test`)과 시안 그림은 같은 자료를
 * 그대로 읽는다. 그래서 시안에서 본 것과 앱에 그려지는 것이 어긋나지 않는다.
 *
 * 모양은 전부 path 하나로 적는다(원·네모도 path 로 바꾼다). 그리는 쪽이 Path 와
 * Text 둘만 알면 된다.
 *
 * 오려 붙인 스티커처럼 보이게, 그리는 쪽이 같은 모양을 세 번 겹친다.
 *   1. 옅은 그림자(아래로 조금 민 것)
 *   2. 흰 테두리(모양마다 `CUT` 만큼 굵은 흰 선을 두른 것)
 *   3. 색 모양
 * 테이프는 반투명 종이라 1·2 를 하지 않는다(`cut: false`).
 */

/** 모양 하나. 글자가 아니면 path 다. */
export type StickerShape =
  | {
      d: string;
      /** 채움색. 없으면 선만 그린다. */
      f?: string;
      /** 선 색과 굵기. 선은 끝과 꺾임이 둥글다. */
      s?: string;
      w?: number;
      /** 불투명도(0~1). */
      o?: number;
      /** SVG transform 글. 회전·축소에만 쓴다. */
      tf?: string;
      /** 점선. */
      da?: string;
    }
  | {
      /** 글씨 스티커의 말. */
      text: string;
      x: number;
      y: number;
      size: number;
      f: string;
    };

export type StickerDrawing = {
  /** viewBox 너비·높이. 너비/높이가 카탈로그의 비율과 같다(시험이 본다). */
  w: number;
  h: number;
  /** 흰 테두리와 그림자를 두르는지. */
  cut: boolean;
  shapes: StickerShape[];
};

/** 흰 테두리 선 굵기(viewBox 단위). 반은 모양 밖으로 나간다. 40 칸 기준 2.5. */
export const CUT = 5;
/** 그림자 색과 세기, 내려 앉는 거리. */
export const SHADOW = { color: "#2B2433", opacity: 0.16, dy: 1.3 } as const;

// ---------------------------------------------------------------- 색

/**
 * 조금 눌러 둔 색. 안쪽 무늬는 같은 색의 짙은 쪽이나 흰색으로 그린다.
 * 글씨 스티커는 흰 글자가 읽히도록 한 단계 짙은 바탕을 쓴다.
 */
const 색 = {
  산호: "#E8766F", 산호짙음: "#B9504C",
  분홍: "#F2A7B8", 분홍짙음: "#CF7189",
  주황: "#F1A95E", 주황짙음: "#C67C35",
  노랑: "#F5CE5E", 노랑짙음: "#CFA23A",
  초록: "#8FC79C", 초록짙음: "#5E9B6D",
  민트: "#84CDBF", 민트짙음: "#4E9D8E",
  하늘: "#95C7E8", 하늘짙음: "#5D95BE",
  파랑: "#7496D6",
  남색: "#5B6DA3",
  보라: "#B6A2DC",
  갈색: "#BD8D69", 갈색짙음: "#8A5E42",
  크림: "#FBF1DF",
  살색: "#F5C9A2", 살색짙음: "#D39A70",
  먹: "#4A4552",
  흰: "#FFFFFF",
  유리: "#E3F2F8",
} as const;

// ---------------------------------------------------------------- 도형 → path

const 수 = (v: number) => +v.toFixed(2);

const 원 = (x: number, y: number, r: number) =>
  `M${수(x - r)} ${수(y)}a${수(r)} ${수(r)} 0 1 1 ${수(2 * r)} 0a${수(r)} ${수(r)} 0 1 1 ${수(-2 * r)} 0Z`;

const 타원 = (x: number, y: number, rx: number, ry: number) =>
  `M${수(x - rx)} ${수(y)}a${수(rx)} ${수(ry)} 0 1 1 ${수(2 * rx)} 0a${수(rx)} ${수(ry)} 0 1 1 ${수(-2 * rx)} 0Z`;

/** 둥근 네모. r 이 0 이면 각진 네모. */
function 네모(x: number, y: number, w: number, h: number, r = 0): string {
  if (r <= 0) return `M${수(x)} ${수(y)}h${수(w)}v${수(h)}h${수(-w)}Z`;
  const k = Math.min(r, w / 2, h / 2);
  return (
    `M${수(x + k)} ${수(y)}h${수(w - 2 * k)}a${수(k)} ${수(k)} 0 0 1 ${수(k)} ${수(k)}` +
    `v${수(h - 2 * k)}a${수(k)} ${수(k)} 0 0 1 ${수(-k)} ${수(k)}h${수(-(w - 2 * k))}` +
    `a${수(k)} ${수(k)} 0 0 1 ${수(-k)} ${수(-k)}v${수(-(h - 2 * k))}a${수(k)} ${수(k)} 0 0 1 ${수(k)} ${수(-k)}Z`
  );
}

/** 하트. (20, 21) 가운데 너비 28 짜리를 k 배로 줄여 (x, y) 에 놓는다. */
function 하트꼴(x: number, y: number, k = 1): string {
  const p = (dx: number, dy: number) => `${수(x + dx * k)} ${수(y + dy * k)}`;
  const r = 수(7.5 * k);
  return (
    `M${p(0, 13)}C${p(0, 13)} ${p(-14, 4.5)} ${p(-14, -5.5)}` +
    `A${r} ${r} 0 0 1 ${p(0, -9.5)}A${r} ${r} 0 0 1 ${p(14, -5.5)}` +
    `C${p(14, 4.5)} ${p(0, 13)} ${p(0, 13)}Z`
  );
}

/** 끝이 뾰족하고 옆이 오목한 네 갈래 반짝임. */
function 반짝꼴(x: number, y: number, R: number): string {
  const k = R * 0.13;
  return (
    `M${수(x)} ${수(y - R)}Q${수(x + k)} ${수(y - k)} ${수(x + R)} ${수(y)}` +
    `Q${수(x + k)} ${수(y + k)} ${수(x)} ${수(y + R)}Q${수(x - k)} ${수(y + k)} ${수(x - R)} ${수(y)}` +
    `Q${수(x - k)} ${수(y - k)} ${수(x)} ${수(y - R)}Z`
  );
}

/** 별 다섯 꼭지. */
function 별꼴(x: number, y: number, R: number, r: number): string {
  const 점: string[] = [];
  for (let i = 0; i < 10; i += 1) {
    const 각 = ((-90 + i * 36) * Math.PI) / 180;
    const 길이 = i % 2 === 0 ? R : r;
    점.push(`${수(x + 길이 * Math.cos(각))} ${수(y + 길이 * Math.sin(각))}`);
  }
  return `M${점.join("L")}Z`;
}

/** 물방울. (x, y) 가 뾰족한 끝이다. */
const 물방울 = (x: number, y: number) =>
  `M${x} ${y}c1.7 2.3 2.5 3.5 2.5 4.6a2.5 2.5 0 0 1-5 0c0-1.1.8-2.3 2.5-4.6Z`;

/** 가운데서 뻗는 짧은 선 n 개. 해의 빛살에 쓴다. */
function 빛살(x: number, y: number, 안: number, 밖: number, n: number): string {
  let d = "";
  for (let i = 0; i < n; i += 1) {
    const 각 = (i * 2 * Math.PI) / n;
    const c = Math.cos(각);
    const s = Math.sin(각);
    d += `M${수(x + 안 * s)} ${수(y - 안 * c)}L${수(x + 밖 * s)} ${수(y - 밖 * c)}`;
  }
  return d;
}

/** 눈송이 여섯 갈래와 곁가지. */
function 눈송이꼴(x: number, y: number, R: number): string {
  let d = "";
  const 끝 = (r: number, 각: number) => `${수(x + r * Math.sin(각))} ${수(y - r * Math.cos(각))}`;
  for (let i = 0; i < 6; i += 1) {
    const 각 = (i * Math.PI) / 3;
    d += `M${수(x)} ${수(y)}L${끝(R, 각)}`;
    const 가지 = R * 0.6;
    const bx = x + 가지 * Math.sin(각);
    const by = y - 가지 * Math.cos(각);
    for (const 틀기 of [-0.8, 0.8]) {
      const 길이 = R * 0.3;
      d += `M${수(bx)} ${수(by)}L${수(bx + 길이 * Math.sin(각 + 틀기))} ${수(by - 길이 * Math.cos(각 + 틀기))}`;
    }
  }
  return d;
}

/** 꽃잎 원 n 개. 순서대로 겹치게 따로 돌려준다. */
function 꽃잎들(x: number, y: number, 거리: number, r: number, n: number): string[] {
  const 잎: string[] = [];
  for (let i = 0; i < n; i += 1) {
    const 각 = (i * 2 * Math.PI) / n;
    잎.push(원(x + 거리 * Math.sin(각), y - 거리 * Math.cos(각), r));
  }
  return 잎;
}

/** 가장자리가 물결인 도장 원. 볼록한 반원 n 개를 잇는다. */
function 도장꼴(x: number, y: number, R: number, n: number): string {
  const 점 = (i: number) => {
    const 각 = (i * 2 * Math.PI) / n;
    return [x + R * Math.sin(각), y - R * Math.cos(각)] as const;
  };
  const 현 = 2 * R * Math.sin(Math.PI / n);
  const r = 수(현 * 0.56);
  const [sx, sy] = 점(0);
  let d = `M${수(sx)} ${수(sy)}`;
  for (let i = 1; i <= n; i += 1) {
    const [px, py] = 점(i);
    d += `A${r} ${r} 0 0 1 ${수(px)} ${수(py)}`;
  }
  return `${d}Z`;
}

/**
 * 마스킹 테이프 몸통. 위아래는 곧고 양 끝은 손으로 찢은 톱니다.
 * 몸통은 x 7~113, 톱니 끝은 4·116 까지 나간다.
 */
function 테이프꼴(): string {
  let d = "M7 5H113";
  for (let j = 1; j <= 8; j += 1) d += `L${j % 2 ? 116 : 113} ${5 + j * 2.5}`;
  d += "L7 25";
  for (let j = 1; j <= 8; j += 1) d += `L${j % 2 ? 4 : 7} ${25 - j * 2.5}`;
  return `${d}Z`;
}

/** 위에서 본 비행기, 기수가 위. 40 칸 가운데. */
const 비행기꼴 =
  "M20 4c2 0 3 2 3 5v7l11 6.5V26l-11-3v7l4 3v3l-7-1.8-7 1.8v-3l4-3v-7L6 26v-3.5L17 16V9c0-3 1-5 3-5Z";

// ---------------------------------------------------------------- 테이프

function 테이프(바탕: string, 무늬: StickerShape[] = []): StickerDrawing {
  return {
    w: 120,
    h: 30,
    cut: false,
    // 종이 결이 비치게 조금 투명하다. 그림 전체가 아니라 모양마다 준다.
    shapes: [{ d: 테이프꼴(), f: 바탕, o: 0.9 }, ...무늬],
  };
}

function 사선줄(): string {
  let d = "";
  for (let x = 10; x <= 100; x += 9) d += `M${x} 25L${x + 6} 5h3.6L${x + 3.6} 25Z`;
  return d;
}

function 점무늬(): string {
  let d = "";
  for (let x = 12; x <= 108; x += 8) d += 원(x, 10, 1.7);
  for (let x = 16; x <= 108; x += 8) d += 원(x, 20, 1.7);
  return d;
}

/** 깅엄. 가로띠와 세로띠를 따로 칠해야 겹친 칸이 한 번 더 짙어진다. */
const 가로띠 = 네모(7, 11.5, 106, 7);
function 세로띠(): string {
  let d = "";
  for (let x = 10; x <= 104; x += 12) d += 네모(x, 5, 6, 20);
  return d;
}

// ---------------------------------------------------------------- 그림

const 네모칸 = (shapes: StickerShape[]): StickerDrawing => ({ w: 40, h: 40, cut: true, shapes });

export const STICKER_ART: Record<string, StickerDrawing> = {
  // ---- 여행
  캐리어: 네모칸([
    { d: "M15 9.5V6.5a2 2 0 0 1 2-2h6a2 2 0 0 1 2 2v3", s: 색.주황짙음, w: 2.4 },
    { d: 네모(9, 9, 22, 24, 4.5), f: 색.주황 },
    { d: "M15.5 13v17M24.5 13v17", s: 색.주황짙음, w: 1.6 },
    { d: 네모(9, 17.5, 22, 3.4), f: 색.크림 },
    { d: 원(14, 35, 2), f: 색.먹 },
    { d: 원(26, 35, 2), f: 색.먹 },
  ]),
  탑승권: {
    w: 64,
    h: 36,
    cut: true,
    shapes: [
      {
        d: "M9 5H40a4 4 0 0 0 8 0H55a4 4 0 0 1 4 4V27a4 4 0 0 1-4 4H48a4 4 0 0 0-8 0H9a4 4 0 0 1-4-4V9a4 4 0 0 1 4-4Z",
        f: 색.하늘,
      },
      { d: "M44 11.5V24.5", s: 색.흰, w: 1.6, da: "0.1 3.2" },
      { d: 비행기꼴, f: 색.흰, tf: "translate(8.5 6.5) scale(0.36) rotate(45 20 20)" },
      { d: 네모(25, 10, 12, 2.6, 1.3), f: 색.흰, o: 0.9 },
      { d: 네모(25, 14.4, 8, 2.6, 1.3), f: 색.흰, o: 0.6 },
      { d: 네모(11, 22.5, 26, 2.6, 1.3), f: 색.흰, o: 0.9 },
      { d: "M50.5 12v12M53.4 12v12M56 12v12", s: 색.하늘짙음, w: 1.4 },
    ],
  },
  카메라: 네모칸([
    { d: 네모(12, 8, 10, 6, 2), f: 색.민트짙음 },
    { d: 네모(5, 11.5, 30, 21.5, 5), f: 색.민트 },
    { d: 원(20, 22.5, 7.4), f: 색.흰 },
    { d: 원(20, 22.5, 4.8), f: 색.먹 },
    { d: 원(18.4, 20.9, 1.4), f: 색.흰 },
    { d: 네모(27, 15, 4.5, 2.8, 1.2), f: 색.크림 },
  ]),
  필름: 네모칸([
    { d: 네모(5, 10, 30, 20, 3), f: 색.먹 },
    ...[0, 1, 2, 3, 4].flatMap((i): StickerShape[] => [
      { d: 네모(7.9 + i * 5.4, 12.2, 2.6, 2.4, 0.6), f: 색.크림 },
      { d: 네모(7.9 + i * 5.4, 25.4, 2.6, 2.4, 0.6), f: 색.크림 },
    ]),
    { d: 네모(8, 16.2, 11, 7.6, 1.2), f: 색.하늘 },
    { d: 네모(21, 16.2, 11, 7.6, 1.2), f: 색.노랑 },
  ]),
  지도핀: 네모칸([
    { d: "M20 36.5S8.5 25 8.5 15.5a11.5 11.5 0 0 1 23 0C31.5 25 20 36.5 20 36.5Z", f: 색.산호 },
    { d: 원(20, 15.5, 4.3), f: 색.흰 },
  ]),
  기차: 네모칸([
    { d: "M14 31l-3.5 5M26 31l3.5 5", s: 색.먹, w: 2.4 },
    { d: 네모(9, 5, 22, 27, 6.5), f: 색.초록 },
    { d: 네모(12, 9, 16, 9.5, 2.5), f: 색.유리 },
    { d: 네모(9, 21, 22, 2), f: 색.초록짙음 },
    { d: 원(14.5, 27, 2), f: 색.크림 },
    { d: 원(25.5, 27, 2), f: 색.크림 },
  ]),
  버스: 네모칸([
    { d: 네모(4, 9, 32, 20, 5), f: 색.노랑 },
    { d: 네모(7.5, 12.5, 7.5, 7, 1.8), f: 색.유리 },
    { d: 네모(17, 12.5, 7.5, 7, 1.8), f: 색.유리 },
    { d: 네모(26.5, 12.5, 6, 7, 1.8), f: 색.유리 },
    { d: 네모(4, 22, 32, 2), f: 색.노랑짙음 },
    { d: 원(12, 29, 3.8), f: 색.먹 },
    { d: 원(28, 29, 3.8), f: 색.먹 },
    { d: 원(12, 29, 1.4), f: 색.크림 },
    { d: 원(28, 29, 1.4), f: 색.크림 },
  ]),
  여권: 네모칸([
    { d: 네모(9, 5, 22, 30, 3.5), f: 색.남색 },
    { d: 원(20, 17, 6.2), s: 색.크림, w: 1.5 },
    { d: "M13.8 17h12.4M20 10.8c-3.6 3.6-3.6 8.8 0 12.4M20 10.8c3.6 3.6 3.6 8.8 0 12.4", s: 색.크림, w: 1.3 },
    { d: 네모(15, 28, 10, 2.2, 1.1), f: 색.크림 },
  ]),
  텐트: 네모칸([
    { d: "M20 4v5", s: 색.먹, w: 1.8 },
    { d: "M20.5 3.5h5l-1.5 1.8 1.5 1.8h-5Z", f: 색.산호, s: 색.산호, w: 1 },
    { d: "M20 9L34.5 32.5H5.5Z", f: 색.주황, s: 색.주황, w: 3 },
    { d: "M20 17l5.5 15.5h-11Z", f: 색.주황짙음, s: 색.주황짙음, w: 1.5 },
  ]),
  // 고르는 자리에서는 뺐다(cardDecor.ts KEEPSAKE_PALETTE). 옛 카드에 붙은 것은 그린다.
  비행기: 네모칸([
    { d: 비행기꼴, f: 색.하늘, s: 색.하늘, w: 2, tf: "rotate(45 20 20)" },
    { d: 원(20, 10.5, 1.5), f: 색.흰, tf: "rotate(45 20 20)" },
  ]),

  // ---- 음식
  커피: 네모칸([
    { d: "M14 11.5c-1.8-1.7 1.8-3.3 0-5M20.5 11.5c-1.8-1.7 1.8-3.3 0-5", s: "#C4C8D0", w: 2 },
    { d: "M27 19.5h2.2a4.3 4.3 0 0 1 0 8.6H27", s: 색.하늘, w: 3 },
    { d: "M7 15h21v13a6 6 0 0 1-6 6h-9a6 6 0 0 1-6-6Z", f: 색.하늘 },
    { d: 타원(17.5, 15, 10.5, 2.6), f: 색.갈색짙음 },
    { d: 하트꼴(17.5, 24.5, 0.22), f: 색.흰 },
  ]),
  맥주: 네모칸([
    { d: "M27 16.5h2.5a4 4 0 0 1 4 4v5a4 4 0 0 1-4 4H27", s: 색.노랑짙음, w: 3 },
    { d: 네모(8, 13, 19, 22, 3.5), f: 색.노랑 },
    { d: "M13 20v11M22 20v11", s: 색.노랑짙음, w: 1.5, o: 0.7 },
    { d: 원(17.5, 22.5, 1.2) + 원(16.8, 28.5, 1) + 원(18.6, 32, 0.8), f: 색.흰, o: 0.85 },
    { d: 원(12, 13, 4) + 원(17.5, 10.8, 4.8) + 원(23, 12.6, 4.2) + 네모(8, 12, 19, 4.5, 2), f: "#FFF5DC" },
  ]),
  아이스크림: 네모칸([
    { d: "M12.5 21L20 37l7.5-16Z", f: "#E3B477", s: "#E3B477", w: 2 },
    { d: "M15.5 24.5l6.5 6.5M24.5 24.5L18 31M20 23l3.5 3.5M20 23l-3.5 3.5", s: "#C18B4C", w: 1.2 },
    { d: 원(20, 14.5, 9), f: 색.분홍 },
    { d: "M11 16.5h18v2.5q-1.5 2.5-3 0q-1.5 3.5-3 0q-1.5 2.5-3 0q-1.5 3.5-3 0q-1.5 2.5-3 0q-1.5 3-3 0Z", f: 색.분홍 },
    { d: "M15.5 10.5l1.6-.9M23.5 16l1.4.9", s: 색.노랑, w: 1.3 },
    { d: "M22.5 8.8l1 1.4M15 15.5l1.2.7", s: 색.민트짙음, w: 1.3 },
    { d: "M19.4 12.2l.3 1.6M25.5 12l1.5-.3", s: 색.흰, w: 1.3 },
  ]),
  떡볶이: 네모칸([
    { d: "M24.5 12L30.5 3.5", s: 색.갈색, w: 1.8 },
    { d: "M7.5 21q2-11.5 12.5-11.5T32.5 21Z", f: "#E0634F" },
    { d: 네모(10.5, 12.8, 10, 3.8, 1.9), f: "#FBE9CC", tf: "rotate(-18 15.5 14.7)" },
    { d: 네모(19, 10.6, 10, 3.8, 1.9), f: "#FBE9CC", tf: "rotate(14 24 12.5)" },
    { d: 네모(14.5, 14.8, 11, 3.8, 1.9), f: "#FBE9CC", tf: "rotate(-4 20 16.7)" },
    { d: "M5 21H35A15 13 0 0 1 5 21Z", f: 색.초록 },
    { d: 네모(4, 19.5, 32, 3, 1.5), f: 색.초록짙음 },
    { d: "M12 27.5h2.5M19 30h2.5M26 27.5h2.5", s: 색.흰, w: 1.3, o: 0.8 },
  ]),
  치킨: 네모칸([
    { d: "M18 22L10.5 29.5", s: "#F2E2C6", w: 4.2 },
    { d: 원(8.3, 28.8, 2.7) + 원(11.2, 31.7, 2.7), f: "#F2E2C6" },
    { d: 타원(24, 16, 11.5, 9), f: "#E2A04C", tf: "rotate(-45 24 16)" },
    { d: "M22 10.5q2.2.6 3.2 2.8M28.5 15.5q1.5 1.2 1.8 3.2M21.5 17q1.5.8 2 2.4", s: "#BF7A2F", w: 1.5 },
  ]),
  케이크: 네모칸([
    { d: 네모(18.6, 8.5, 2.8, 10, 1), f: 색.하늘 },
    { d: "M20 2.8c2 2.4 2.6 3.6 2.6 4.6a2.6 2.6 0 0 1-5.2 0c0-1 .6-2.2 2.6-4.6Z", f: 색.주황 },
    { d: 네모(7, 18, 26, 16, 4), f: 색.분홍 },
    {
      d: "M7 22q0-4 4-4h18q4 0 4 4v1q-2.17 2.5-4.33 0q-2.17 2.5-4.33 0q-2.17 2.5-4.33 0q-2.17 2.5-4.33 0q-2.17 2.5-4.33 0q-2.17 2.5-4.33 0Z",
      f: 색.크림,
    },
    { d: 원(12, 29, 1.2) + 원(20, 29.8, 1.2) + 원(28, 29, 1.2), f: 색.흰 },
  ]),
  소주: 네모칸([
    { d: 네모(12.6, 4, 5.3, 3, 1), f: 색.초록짙음 },
    {
      d: "M13 5.5h4.5v5.5q3.5 2 3.5 6.5v14a3 3 0 0 1-3 3H12.5a3 3 0 0 1-3-3v-14q0-4.5 3.5-6.5Z",
      f: "#7DBF93",
    },
    { d: 네모(9.5, 20, 11.5, 8), f: 색.크림 },
    { d: "M12.5 23h5.5M12.5 25.5h3.5", s: 색.초록짙음, w: 1.1 },
    { d: "M22.5 21h11l-1.3 13a1.5 1.5 0 0 1-1.5 1.3h-5.4a1.5 1.5 0 0 1-1.5-1.3Z", f: "#D4ECF2" },
    { d: "M23.3 26h9.4l-.8 8.3H24.1Z", f: "#A9D8E3" },
  ]),
  수박: 네모칸([
    { d: "M5 12.5h30a15 15 0 0 1-30 0Z", f: 색.초록 },
    { d: "M7.4 12.5h25.2a12.6 12.6 0 0 1-25.2 0Z", f: "#F4F1DC" },
    { d: "M9.2 12.5h21.6a10.8 10.8 0 0 1-21.6 0Z", f: "#EE7B78" },
    {
      d: 타원(14.5, 16.5, 0.9, 1.4) + 타원(20, 19.5, 0.9, 1.4) + 타원(25.5, 16.5, 0.9, 1.4) + 타원(17, 22.5, 0.9, 1.4) + 타원(23, 22.5, 0.9, 1.4),
      f: 색.먹,
    },
  ]),

  // ---- 날씨
  해: 네모칸([
    { d: 빛살(20, 20, 12.2, 15.5, 8), s: 색.주황, w: 3 },
    { d: 원(20, 20, 8.4), f: 색.노랑 },
    { d: 원(17.3, 17.3, 2.2), f: 색.흰, o: 0.5 },
  ]),
  구름: 네모칸([
    { d: 원(13, 25, 7) + 원(21, 19, 9) + 원(28, 25, 7) + 네모(13, 24, 15, 8), f: "#B9D7EE" },
  ]),
  비: 네모칸([
    { d: 원(13, 17, 6) + 원(20.5, 12.5, 7.5) + 원(27, 17, 6) + 네모(13, 16, 14, 7), f: "#AEBFD2" },
    { d: 물방울(13, 26) + 물방울(20, 28.5) + 물방울(27, 26), f: 색.파랑 },
  ]),
  무지개: 네모칸([
    { d: "M7 27a13 13 0 0 1 26 0", s: 색.산호, w: 3.6 },
    { d: "M10.4 27a9.6 9.6 0 0 1 19.2 0", s: 색.노랑, w: 3.6 },
    { d: "M13.8 27a6.2 6.2 0 0 1 12.4 0", s: 색.하늘, w: 3.6 },
    { d: 원(7, 28.5, 3.4) + 원(10.6, 29.5, 2.8) + 원(33, 28.5, 3.4) + 원(29.4, 29.5, 2.8), f: "#EEF1F6" },
  ]),
  눈송이: 네모칸([{ d: 눈송이꼴(20, 20, 14), s: "#8DB7DF", w: 2.8 }]),
  달: 네모칸([
    { d: "M17.75 6A14 14 0 1 0 31.28 24.44A11.5 11.5 0 0 1 17.75 6Z", f: 색.노랑 },
    { d: 원(11.5, 22, 2) + 원(16.5, 29, 1.4) + 원(10.5, 14.5, 1.2), f: 색.노랑짙음, o: 0.55 },
    { d: 반짝꼴(31, 11, 3.8), f: 색.노랑, s: 색.노랑, w: 0.8 },
  ]),
  바람: 네모칸([
    { d: "M6 15h17a4 4 0 1 0-4-4", s: 색.민트, w: 3.2 },
    { d: "M6 22h24a4.5 4.5 0 1 1-4.5 4.5", s: 색.민트, w: 3.2 },
    { d: "M6 29h11", s: 색.민트, w: 3.2 },
  ]),
  우산: 네모칸([
    { d: "M20 7.5V4.5", s: 색.먹, w: 2 },
    { d: "M20 19v11.5a3 3 0 0 1-6 0", s: 색.먹, w: 2.2 },
    {
      d: "M6 20a14 13 0 0 1 28 0q-2.33-2.5-4.67 0q-2.33-2.5-4.67 0q-2.33-2.5-4.67 0q-2.33-2.5-4.67 0q-2.33-2.5-4.67 0q-2.33-2.5-4.67 0Z",
      f: 색.산호,
    },
    { d: "M20 7.5q-4 5-4.67 12.5M20 7.5q4 5 4.67 12.5", s: 색.산호짙음, w: 1.2 },
  ]),

  // ---- 기분
  하트: 네모칸([
    { d: 하트꼴(20, 21), f: "#EE8290" },
    { d: 타원(12.8, 15.2, 2.4, 1.5), f: 색.흰, o: 0.55, tf: "rotate(-40 12.8 15.2)" },
  ]),
  별: 네모칸([
    { d: 별꼴(20, 21.5, 14.5, 7), f: 색.노랑, s: 색.노랑, w: 3 },
    { d: 원(16.5, 17.5, 1.6), f: 색.흰, o: 0.5 },
  ]),
  반짝: 네모칸([
    { d: 반짝꼴(18, 22, 14), f: "#F6D06A", s: "#F6D06A", w: 1.5 },
    { d: 반짝꼴(31, 9.5, 5.5), f: "#F6D06A", s: "#F6D06A", w: 1.2 },
  ]),
  엄지척: 네모칸([
    { d: 네모(6, 17.5, 6.5, 16, 2), f: 색.파랑 },
    { d: "M13 19.5L17.5 9C18.5 6.5 22.8 7.2 22.3 10.5L21.5 18Z", f: 색.살색, s: 색.살색, w: 1 },
    { d: 네모(12, 18, 17.5, 15.5, 4), f: 색.살색 },
    { d: "M29.5 22.5h-6.5M29.5 26.5h-6.5M29 30.5h-6", s: 색.살색짙음, w: 1.4 },
  ]),
  "웃는 얼굴": 네모칸([
    { d: 원(20, 20, 14), f: 색.노랑 },
    { d: "M13.5 18.5q2-2.8 4 0M22.5 18.5q2-2.8 4 0", s: 색.갈색짙음, w: 2 },
    { d: "M15 23.5q5 5 10 0", s: 색.갈색짙음, w: 2 },
    { d: 타원(11.5, 23.5, 2.3, 1.6) + 타원(28.5, 23.5, 2.3, 1.6), f: "#F29C8E", o: 0.7 },
  ]),
  느낌표: 네모칸([
    { d: "M16.5 8.5a3.5 3.5 0 0 1 7 0L22.3 24a2.3 2.3 0 0 1-4.6 0Z", f: 색.산호 },
    { d: 원(20, 31, 3.2), f: 색.산호 },
  ]),
  체크: 네모칸([
    { d: 원(20, 20, 14), f: 색.초록 },
    { d: "M13 20.5l4.8 4.8L27.5 15", s: 색.흰, w: 3.4 },
  ]),
  꽃: 네모칸([
    ...꽃잎들(20, 20, 8.5, 6.4, 5).map((d): StickerShape => ({ d, f: 색.분홍, s: 색.분홍짙음, w: 0.8 })),
    { d: 원(20, 20, 5), f: 색.노랑 },
    { d: 원(18.6, 18.6, 1.2), f: 색.흰, o: 0.6 },
  ]),
  말풍선: 네모칸([
    { d: "M12 26L10 33.5L19.5 27.5Z", f: 색.보라, s: 색.보라, w: 1.5 },
    { d: 네모(5, 7, 30, 21, 8), f: 색.보라 },
    { d: 원(13.5, 17.5, 2) + 원(20, 17.5, 2) + 원(26.5, 17.5, 2), f: 색.흰 },
  ]),

  // ---- 글씨. 글자 크기와 자리는 쿠키런 Bold 기준, 한글 한 자가 대략 글자 크기만큼 넓다.
  "최고의 하루": {
    w: 104,
    h: 30,
    cut: true,
    shapes: [
      { d: "M5 4H99L93 15L99 26H5L11 15Z", f: 색.노랑, s: 색.노랑, w: 2 },
      { text: "최고의 하루", x: 52, y: 19.7, size: 13, f: "#6B4A1E" },
    ],
  },
  "여행 중": {
    w: 72,
    h: 30,
    cut: true,
    shapes: [
      { d: 네모(4, 4, 64, 22, 11), f: "#4FA395" },
      { d: 원(15, 15, 2.6), f: 색.흰 },
      { text: "여행 중", x: 40.5, y: 19.5, size: 12.5, f: 색.흰 },
    ],
  },
  맛집: 네모칸([
    { d: 도장꼴(20, 20, 14, 14), f: "#E26D66" },
    { d: 원(20, 20, 10.8), s: 색.흰, w: 1, da: "1.6 1.6" },
    { text: "맛집", x: 20, y: 23.3, size: 9, f: 색.흰 },
  ]),
  "또 오자": {
    w: 72,
    h: 34,
    cut: true,
    shapes: [
      { d: "M17 5H64a4 4 0 0 1 4 4V25a4 4 0 0 1-4 4H17L6 17Z", f: "#6E8FD0", s: "#6E8FD0", w: 2 },
      { d: 원(14.5, 17, 2.3), f: 색.흰 },
      { text: "또 오자", x: 42, y: 21.5, size: 12.5, f: 색.흰 },
    ],
  },
  디데이: {
    w: 64,
    h: 32,
    cut: true,
    shapes: [
      { d: 네모(4, 4, 56, 24, 8), f: 색.먹 },
      { text: "D-DAY", x: 32, y: 20.7, size: 13, f: 색.노랑 },
    ],
  },
  우리: 네모칸([
    { d: 하트꼴(20, 21), f: "#E88AA0" },
    { text: "우리", x: 20, y: 22.8, size: 10, f: 색.흰 },
  ]),
  행복: {
    w: 52,
    h: 40,
    cut: true,
    shapes: [
      { d: "M14 27L12 35L21.5 28Z", f: "#E8914A", s: "#E8914A", w: 1.5 },
      { d: 네모(4, 4, 44, 25, 11), f: "#E8914A" },
      { d: 반짝꼴(41.5, 10.5, 3), f: 색.흰 },
      { text: "행복", x: 26, y: 21.2, size: 13, f: 색.흰 },
    ],
  },

  // ---- 테이프
  "줄무늬 테이프": 테이프("#FBEBD3", [{ d: 사선줄(), f: "#EE9E95", o: 0.85 }]),
  "도트 테이프": 테이프("#BFDCEF", [{ d: 점무늬(), f: 색.흰, o: 0.9 }]),
  "체크 테이프": 테이프("#FBE0E5", [
    { d: 가로띠, f: "#EE9AAE", o: 0.4 },
    { d: 세로띠(), f: "#EE9AAE", o: 0.4 },
  ]),
  "베이지 테이프": 테이프("#E9D6B4"),
  "민트 테이프": 테이프("#A9DCCF"),
  "분홍 테이프": 테이프("#F4BFCB"),
};

/** 그림이 있는 이름 전부. 카탈로그 시험이 이것과 맞춰 본다. */
export const ART_KEYS: readonly string[] = Object.keys(STICKER_ART);
