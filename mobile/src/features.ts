/**
 * 아직 열지 않은 기능.
 *
 * 여기 값이 `false` 면 앱 어디에도 그 기능으로 들어가는 길이 보이지 않는다.
 * 번들에는 코드가 들어 있어도 버튼과 메뉴가 나타나지 않는다.
 *
 * expo 나 react-native 를 가져오지 않는다. `node --test` 로 바로 시험한다.
 */

/**
 * 카카오톡 공지를 통째로 붙여넣어 지난 여행을 채우는 기능.
 *
 * 스토어 출시 뒤에 유료로 연다. 그때 이 값을 `true` 로 바꾸면 「여행」 탭의
 * 여행 목록에 "공지 붙여넣기" 가 나타난다. 그 밖에 손댈 곳은 없다.
 */
export const NOTICE_IMPORT_ENABLED = false;
