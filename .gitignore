node_modules/
.expo/
dist/
.DS_Store
*.log

# 폰트 원본 내려받기 작업 폴더
mobile/.fontsrc/

# 네이티브 폴더는 커밋하지 않고 빌드 전에 `npx expo prebuild` 로 다시 만든다
mobile/ios/
mobile/android/

# prebuild 를 저장소 루트에서 돌리면 여기에도 생긴다. 생성물이라 커밋하지 않는다
/ios/
/android/

# 실제 환경변수 파일은 커밋하지 않는다. .env.example 만 남긴다
.env
.env.local
!.env.example

# daymo.xyz 사이트: 운영자 이름(공개 저장소에 넣지 않는다)과 빌드 결과, Vercel 연결 정보
site/site.local.json
site/dist/
site/.vercel/
site/.env*

# 로컬 개발 서버가 받은 사진
backend/uploads/

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
