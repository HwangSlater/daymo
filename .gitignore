node_modules/
.expo/
dist/
.DS_Store
*.log

# 폰트 원본 내려받기 작업 폴더
mobile/.fontsrc/

# 네이티브 폴더는 커밋하지 않고 빌드 전에 `npx expo prebuild` 로 다시 만든다
mobile/ios/
mobile/android/

# 실제 환경변수 파일은 커밋하지 않는다. .env.example 만 남긴다
.env
.env.local
!.env.example

server/.gradle/
server/build/
