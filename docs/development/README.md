# Daymo 개발 문서

이 폴더는 현재 UI 프로토타입을 실제 iOS·Android 서비스로 전환하기 위한 기준 문서다. 기존 요구사항은 `../01-requirements.md`, 실제 사용 예시는 `../02-raw-travel-data.md`를 원문으로 유지한다.

## 문서 순서

1. [개발 환경](./01-development-environment.md): 로컬 환경, 환경 변수, 브랜치와 배포
2. [시스템·데이터 설계](./02-architecture-and-data-model.md): 앱 구조, 도메인, 권한, 동기화
3. [API 명세](./03-api-specification.md): 화면별 API와 요청·응답 계약
4. [단계별 개발 계획](./04-implementation-roadmap.md): 구현 순서, 완료 기준, 승인 지점
5. [품질·운영 기준](./05-quality-and-operations.md): 테스트, 성능, 보안, 모니터링
6. [VPS 운영](./06-vps-deployment.md): 2GB 서버 구성, 배포, 백업과 용량 정책, 미니PC/NAS 이전
7. [기기 저장·동기화](./07-local-first-and-sync.md): 로컬/서버 책임, 증분 동기화, 데이터 절약
8. [개인정보·출시 준수](./08-privacy-and-release-compliance.md): 동의, 처리방침, 권한, 탈퇴, 스토어 신고
9. [UI-개발 추적표](./09-ui-development-traceability.md): 현재 화면별 데이터·API·로컬 처리·테스트 연결
10. [개발 착수 준비](./10-implementation-readiness.md): 현재 차이, 결정 목록, 0단계 실행 순서와 완료 게이트
11. [소유자 준비 안내](./11-owner-setup-guide.md): 개발 환경, 계정, 시크릿과 로컬 사진 저장 선택

## 문서 상태

- 기준 UI: `main` 브랜치 `164905d` 이후 UI를 기준으로 하며 개발 시작 시 태그로 고정
- 앱 버전: `0.1.0`
- 마지막 개발 준비 감사: 2026-08-14
- 현재 상태: 이메일 인증, 공간·여행 조회·생성·수정, 공간 멤버 표시, 계정 삭제, 여행 참가자와 장소·일정·숙소·교통편·예약·비용·준비·요리가 API에 연결됐다. 기록(메모·일기·사진)과 출시 준비가 다음 단계다. 자세한 것은 아래 [현재 위치](#현재-위치).
- 목표: UI를 유지하면서 실제 인증, 공동 공간, 여행 데이터, 파일 저장과 동기화를 순차 연결한다.
- 백엔드 기준: Python 3.13 + FastAPI, iwinv VPS(2vCPU/2GB RAM/50GB NVMe/일 20GB·월 600GB 트래픽)

## 현재 위치

**마지막 갱신: 2026-09-15.** 이어서 할 때 여기부터 읽는다.

### 된 것

| | |
| --- | --- |
| 스키마 | 테이블 36개, migration 18개. `02` 문서의 여행 도메인 전체와 계정·인증·시도 제한 |
| 인증 | 이메일 가입·확인·로그인·갱신·로그아웃·비밀번호 재설정, 세션 목록, 재인증 증표(틀린 비밀번호 시도 제한) |
| 계정 삭제 | `DELETE /me` 7일 유예·모든 기기 로그아웃·안내 메일, 유예 중 재인증 취소, 기한 뒤 비식별화 정리 작업(`app.jobs.cleanup`) |
| 앱 계정 삭제 | 설정 > 내 프로필 > 계정 삭제, 다시 로그인하면 삭제 취소 화면. 로컬 웹 빌드로 요청·관리자 막힘·틀린 비밀번호·취소를 눌러 확인했다 |
| 심사 대비 | 서버에 키가 있는 제공자만 소셜 로그인 버튼을 보임, iPhone 전용, 암호화 수출 신고 값, 쓰지 않는 카메라·마이크 권한 제거 |
| 앱 인증 | 운영 HTTPS API 로그인, SecureStore 세션, 재실행 시 refresh 회전, 오프라인 프로필 복구 |
| 앱 공간·여행 | 첫 공간 생성, 공간·여행 목록 조회, 여행 생성·수정, 마지막 데이터 캐시 |
| 앱 공간 정보 | 실제 멤버와 권한 표시, 공간 이름·관계·함께한 날을 서버에 저장(관리자만), 저장 실패 안내, 이름으로 멤버 추가 막음 |
| 공간·여행 | 공간 만들기·목록·수정·멤버 목록, 여행 만들기·조회·수정·참가자·보관·삭제·복구 |
| 지킴 | 시도 제한(계정·IP 따로), refresh token 회전과 재사용 시 가족 폐기, 기기 5대 한도 |
| 검사 | `pytest` 461개, CI에서 서버 job이 PostgreSQL 컨테이너로 돈다 |

`/v1` 아래 경로 54개가 열려 있다. `uv run python dev.py` 로 띄우고
`http://127.0.0.1:8000/docs` 에서 볼 수 있다.

### 다음에 할 일

1. ~~**공간 정보를 앱에 연결한다.**~~ 끝났다(2026-09-15). 초대 링크·권한 변경·관리자 넘기기·
   내보내기·나가기도 붙였다(2026-09-16, 아래 "초대와 멤버").
2. ~~**여행 참가자를 서버에 연결한다.**~~ 끝났다(2026-09-15). 여행 만들기·여행 수정·비용 탭의
   참가자가 membership id로 저장되고, 버전이 어긋나면 최신 여행을 받아 다시 보여 준다.
3. **여행 하위 데이터 API를 만든다.** `장소 → 일정·숙소 → 비용 → 준비·요리 → 기록`
   순으로 연결한다. 표는 이미 있고 API가 아직 없다.
4. **로컬 우선 동기화를 완성한다.** 하위 기록을 SQLite에 저장하고 outbox·증분 sync로
   오프라인 작성, 재시도, 공동 편집 충돌을 처리한다.
5. **출시 준비를 마친다.** 권한 문구·개인정보 처리방침·계정 삭제를 실기기에서 점검하고,
   iPhone·Android 스크린샷과 스토어 메타데이터를 새 UI로 다시 만든 뒤 TestFlight와
   Play 내부 테스트를 통과시킨다.

**3번(여행 하위 데이터 API)은 끝났다(2026-09-16).** 장소, 일정·숙소, 교통편·예약, 비용(지출·정산), 준비·요리,
사람 묶기(같은 이름·이름 변경·나간 멤버), 메모·일기, 사진·영수증이 서버와 맞는다.
**다음 시작점은 실기기 확인이 필요한 것들과 5번(출시 준비)이다.** 사진 받기(다른 기기의 사진을 폰에 받아 두기)와
초대 링크로 앱 열기(`daymo://invite`)는 웹 빌드로 확인할 수 없어 새 빌드가 필요하다. 빌드는 요청이 있을 때만 만든다.
표시 이름은 설정 → 내 프로필에서 고치면 잠깐 뒤 서버(`PATCH /me`)에 저장되고, 멤버의 기기는 다음에 멤버 목록을 받을 때 기록 속 이름을 따라 바꾼다.

초대와 멤버(2026-09-16):

- **초대 링크**는 `https://api.daymo.xyz/auth/invite?token=...`. 페이지가 공간을 드러내지 않고 `daymo://invite?token=...`으로 앱을
  연다. 앱은 링크로 열리면 token을 받아 두었다가 로그인 뒤 "참여할까요?"를 묻는다. 앱이 안 열리면 우리 → 멤버 관리 →
  "초대 링크로 참여"에 붙여 넣는다(`mobile/src/inviteLink.ts`가 글에 섞인 링크에서도 token을 꺼낸다)
- **초대 링크 보내기**(owner·editor)는 링크를 만들자마자 복사하고 공유 창을 연다. 원문은 그때만 받을 수 있다
- **관리자**는 고른 멤버의 권한(편집 가능·보기만)을 바꾸고, 관리자를 넘기고, 내보낸다. 누구나 나갈 수 있고,
  다른 멤버가 있는 관리자는 서버가 먼저 넘기라고 답한다
- **고친 것:** 공간 목록을 다시 받을 때 처음 읽은 설정의 공간과 비교해, 초대로 들어간 공간으로 옮겨 가자마자 첫 공간으로 되돌아가던 문제
- 확인: 로컬 웹 빌드로 다른 계정이 만든 링크를 글과 함께 붙여 넣어 참여 → editor로 들어가고 그 공간으로 옮겨 감,
  들어간 공간에서 초대 링크 만들기, 초대 페이지 200·앱 링크 포함

3번 중 사진을 하며 남긴 것:

- **서버:** `POST /trips/{id}/photos`(줄과 한도) → `PUT /photos/{id}/content`(파일 그대로) 두 번에 올린다. 서버가 SHA-256을 맞추고
  EXIF를 뺀 표시본(1440px)·썸네일(480px)을 만든다. 파일은 `/srv/daymo/uploads/trips/{여행}/{사진}/`, 전달은 API의 `FileResponse`.
  지운 사진은 7일, 멈춘 올리기는 하루 뒤 정리 작업이 파일째 지운다. VPS 사진 폴더 소유자를 컨테이너 uid 10001로 맞췄다
- **앱:** 코덱은 `mobile/src/photoSync.ts`(설명·날짜만 오간다), 파일은 `photoTransfer.ts`. 폰은 `FileSystem.uploadAsync`로 파일을 JS에
  올리지 않고 보낸다. 다른 기기의 사진은 표시본을 문서 폴더에 받아 둔다(폰만). 사진 자체를 바꾸면 새 사진으로 올린다
- **영수증:** 지출의 영수증 파일을 사진(`isReceipt`)으로 올린 뒤 지출에 `receiptPhotoId`를 붙인다. 다른 기기가 붙인 영수증은 받아 둔다
- **로그인 풀림 고침:** 토큰 갱신 뒤 다시 보낸 요청이 422 등으로 실패해도 로그인을 풀던 문제를 고쳤다(`withAccessToken`)
- **아직 없는 것:** 휴지통 조회·복원, 중복 사진 안내, 사용량 화면, X-Accel-Redirect, 올린 사람이 아닌 editor가 남의 사진을 고칠 때의
  안내 문구(지금은 보기만 권한 안내가 뜬다)
- 확인: 로컬 웹 빌드로 옛 사진(파일 있음)·예시 사진(파일 없음)·영수증 지출 옮기기 → 파일 있는 사진과 영수증만 올라감, 썸네일·영수증
  받기 200, 다른 기기의 사진이 다시 열면 목록에 보임, 남의 사진 수정 403

3번 중 메모·일기를 하며 남긴 것:

- **메모와 일기가 서버에 간다.** 코덱은 `mobile/src/memorySync.ts`. 메모의 작성자 줄(`하늘 · 오늘 10:42 · 수정됨`)은
  서버의 작성자와 시각으로 만들고, 앱은 본문만 보낸다. 예전에는 새 메모의 작성자가 `하늘`로 박혀 있었다
- **일기의 날짜는 `writtenOn`(그 일기가 다루는 날).** 여행 중에 쓰면 오늘이고, 여행 밖에서 쓰면 비워 두고 쓴 날로 보인다.
  제목을 비우면 비운 채 저장하고 화면에서만 `이번 여행 이야기`로 보인다
- **메모를 지우면 서버에 행이 남는다.** 7일 안에는 메모 시트의 휴지통에서 되돌린다(사진도 함께)
- 확인: 로컬 웹 빌드로 옛 메모·일기 옮기기, 다른 기기의 메모 추가·고침·일기 추가가 다시 열면 작성자·수정됨·날짜와 함께 보임

사람 묶기를 하며 남긴 것:

- **화면은 계속 이름을 들고, 이름이 곧 한 사람이 되게 했다.** 이름을 id로 통째로 바꾸면 상세 화면 전체를 고쳐야 해서,
  대신 사람 표(`rosterOf`)의 이름을 겹치지 않게 만들고(`하늘 2`, `공용`·`미정`·`구매`와 같은 이름도 번호) 여행마다
  `personNames`(`membership id → 이름`)를 저장한다. 코드는 `mobile/src/people.ts`
- **멤버가 이름을 바꾸면 여행을 열 때 기록 곳곳의 옛 이름을 새 이름으로 바꾼다**(참가자·준비물·재료·교통편·지출·몫·주고받은 기록).
  두 사람이 이름을 맞바꿔도 한꺼번에 바꿔 섞이지 않는다. 표를 아직 못 받은 채로 열면 다음에 연다
- **나간 멤버도 표에 넣는다**(`GET /spaces/{id}/members?includeLeft=true`). 나간 사람이 낸 지출·맡은 준비물이 이름 그대로 보이고
  서버에도 올라간다. "우리" 탭 멤버 목록에는 나오지 않는다. 이미 참가자인 나간 멤버는 참가자 교체 때 빠지지 않는다
- **이름 바꾸기 API는 아직 없다.** 확인은 DB에서 별명을 바꿔 했다
- 확인: 로컬 웹 빌드로 여울 별명을 바꾸고 새봄을 내보낸 뒤, 옛 이름으로 적힌 오프라인 지출이 새 이름으로 바뀌어 여울의 것으로
  올라감, 새봄이 맡은 준비물이 그대로 보이고 서버 담당도 유지

3번 중 준비·요리를 하며 남긴 것:

- **준비물과 요리가 서버에 간다.** 코덱은 `mobile/src/cookingSync.ts`. 앱은 체크를 목록 밖(`packingDone`,
  `cookingReadyIngredientIds`)에 두므로 맞출 때만 줄에 붙여 보고, 서버 모습이 오면 둘로 다시 나눈다
- **여행마다 준비물 목록 하나를 서버가 처음 쓸 때 만든다.** 체크는 `completed` 참/거짓으로 오가고 누가 했는지는 서버에만 남는다
- **재료는 요리와 함께 배열로 오간다.** 서버는 id가 같은 재료를 고치고 빠진 재료만 지운다. 재료 id도 UUID여야 올라가서,
  준비물·요리·재료의 새 id를 모두 `Crypto.randomUUID()`로 바꾸고 옛 id는 여행을 열 때 한 번 옮긴다
- **담당:** 준비물의 `공용`은 `isShared`, `미정`은 비움. 재료의 `구매`는 `procurement=buy`, 사람은 `bring`
- **아직 없는 것:** 재료를 준비물로 가져온 연결(`sourceIngredientId`)을 앱이 보내지 않는다. 일괄 API와 중복 경고도 없다
- 확인: 로컬 웹 빌드로 옛 준비물 3개·요리 1개(재료 2) 옮기기, 다시 기다려도 버전이 오르지 않음, 다른 기기의 체크·재료 준비
  해제·요리 추가가 다시 열면 보임, 새 기기에서 서버 목록 받기, 화면에서 체크·여러 개 추가가 서버에 저장

3번 중 비용을 하며 남긴 것:

- **지출·주고받은 기록·비용 설정이 서버에 간다.** 코덱은 `mobile/src/expenseSync.ts`. 비용 설정(통화·환율·예산·정산 묶기)은
  여행 단위라 목록 동기화가 아니라 `PATCH /trips/{id}/expense-settings`로 보내고, 버전이 어긋나면 최신 버전으로 한 번 더 보낸다
- **처음 연 여행은 기기의 비용 설정을 올리고, 그 뒤로는 서버 값으로 연다**(`expenseSettingsSynced`)
- **공간 사람 표에 없는 이름이 섞인 지출·기록은 올리지 않는다.** 이름 하나를 빼고 올리면 몫이 달라진다. 서버에서 받은
  나간 멤버의 지출은 `나간 멤버`로 보이고 기기에서 고쳐도 서버에 가지 않는다
- **동기화 틀 수정:** 올릴 수 없는 줄도 목록에 있으면 서버에서 지우지 않고, 서버에 같은 id가 있으면 두 번 보이지 않는다
- **영수증 사진은 아직 기기에만 있다**(사진 업로드는 기록 단계에서)
- **송금 기록 되돌리기는 서버에 행을 남기고 누가 되돌렸는지 적는다.** audit log(`payment.undo`)도 남긴다
- 확인: 로컬 웹 빌드로 옛 지출·기록·JPY 설정 옮기기, 다른 기기가 바꾼 예산과 더한 지출이 다시 열면 보임

3번 중 교통편·예약을 하며 남긴 것:

- **서버에 날짜 칸을 따로 뒀다**(`travel_on`·`reserved_on`). 날짜만 있고 시각이 없는 줄을 timestamptz 하나로 적을 수 없다
- **밤 비행기·기차:** 도착 시각이 출발보다 이르면 다음 날 도착으로 저장한다
- **교통편의 탈 사람은 이름 ↔ membership id.** 공간에 없는 이름(옛 기록의 `동행`, 나간 멤버)은 정하지 않은 것으로 올리고,
  다시 열면 비어 보인다
- **예약 인원은 적은 글자 그대로 둔다**(`partyLabel`). 숫자로 읽히면 `partySize`에도 넣는다. 앱의 예약 장소(자유 글자)는 서버 메모 칸이다
- **교통편·예약에서 만든 일정 줄은 목록이 바뀔 때 다시 만든다.** 예전에는 저장 버튼에서만 만들어 다른 기기의 교통편·예약이
  일정에 보이지 않았다. 그 줄을 일정 화면에서 고쳐도 원래 모습으로 돌아간다(예전과 같다)
- 확인: 로컬 웹 빌드로 옛 교통편·예약 옮기기(탈 사람·인원 글자 보존, 일정 연결 끊김 0), 다른 기기가 넣은 오는 편이
  다시 열면 일정 줄로 보임

3번 중 일정·숙소를 하며 남긴 것:

- **동기화 틀을 일반화했다.** `mobile/src/listSync.ts`(계산)와 `useListSync.ts`(훅)가 목록 하나를 맞추고,
  목록마다 다른 모양은 코덱으로 받는다. 장소는 `placeSync.ts`, 일정·숙소는 `scheduleSync.ts`.
  교통편·예약·비용도 코덱과 API 네 개만 더하면 된다
- **날짜·시각은 공간 시간대로 서버가 계산한다.** 앱은 화면 글자(`2일(금)`·`금 · 12:30`,
  `10월 1일 15:00`)를 여행 기간으로 풀어 `YYYY-MM-DD`·`HH:MM`으로 보낸다
- **숙소·예약·교통편에서 만든 일정 줄은 올리지 않는다.** 그쪽의 `showInSchedule`로 다시 만들어진다
- **일정의 장소 연결은 장소가 서버에 올라간 뒤에 잇는다.** 그 전에는 비워 보내고, 장소가 올라가면
  다시 보내 잇는다(한 번 더 고치기가 생긴다)
- **숙소는 대표 숙소 하나만 맞춘다.** 서버 숙소에는 이름·주소가 없어 이은 장소의 것을 쓴다
- **여행 기간을 바꾸면 서버의 `trip_days`도 따라간다.** 예전에는 늘린 날에 일정을 붙일 수 없었다
- 확인: 로컬 웹 빌드로 옛 기록(일정 2줄·숙소·장소 2곳) 옮기기, 다른 기기가 고치고 더한 일정이 다시 열면
  보임, 앱에서 일정 추가, 대표 숙소 해제가 서버에서도 지워짐

3번 중 장소를 하며 남긴 것:

- **장소는 서버와 맞춘다.** `GET/POST /trips/{id}/places`, `PATCH/DELETE /trip-places/{id}`.
  앱은 화면 곳곳의 장소 수정 코드를 그대로 두고, `mobile/src/usePlaceSync.ts` 가 목록이 바뀔 때마다
  마지막으로 맞춘 모습과 비교해 만들기·고치기·지우기를 보낸다(계산은 `placeSync.ts`).
  일정·비용·준비물도 같은 틀로 옮길 수 있게 나눠 두었다
- **장소 id는 앱이 만든 UUID를 서버가 그대로 쓴다.** 다시 보낸 요청은 하나만 만든다. 옛 기기 기록의
  `place-…` id는 처음 열 때 새 UUID로 바꾸고 일정·숙소가 가리키는 id도 함께 바꾼다
- **처음 열 때 합치는 규칙:** 서버에 있으면 서버 것, 기기에만 있고 예전에 맞춘 적이 있으면(`placeSyncIds`)
  다른 곳에서 지운 것이라 버림, 맞춘 적이 없으면 못 올린 것이라 올림
- **아직 없는 것:** 오프라인에서 고친 기존 장소는 다시 열 때 서버 모습이 이긴다(4번 outbox에서 해결).
  보기만 하는 멤버도 기기 화면에서는 장소를 고칠 수 있고 저장만 거부된다. 장소 순서는 서버에 없다
- 확인: 로컬 웹 빌드로 담기(앱 UUID 그대로), 고치기(v2), 다른 기기가 먼저 고친 뒤 고치기(최신 목록으로
  되돌림), 다른 기기가 담은 장소가 다시 열면 보임, 지우기, 옛 `place-` 기록 3개 옮기기(끊긴 연결 0)

2번을 하며 남긴 것:

- **앱 안의 참가자는 여전히 이름이다.** 지출의 낸 사람·몫, 준비물 담당이 이름으로 적혀 있어서
  서버와 오갈 때만 `mobile/src/tripSync.ts` 의 표로 이름과 id를 바꾼다. 멤버가 서버에서 이름을
  바꾸면 기기 기록 속 옛 이름과 어긋난다. 하위 기록이 서버로 가는 3번에서 id로 바꾼다
- **같은 이름의 멤버가 둘이면** 앞에서부터 하나씩 짝짓는다. 화면에서도 둘을 구분할 수 없다
- **서버 참가자가 비어 있으면 "공간 멤버 전원"이다**(API 명세). 앱은 이때 기기의 참가자를 그대로 둔다
- 확인: 로컬 웹 빌드로 만들기 → 서버 3명, 비용 탭에서 빼기 → 2명(version 2), 다른 기기가 먼저
  제목을 고친 뒤 수정 → 최신 제목으로 되돌아옴, 다른 기기가 참가자를 바꾼 뒤 다시 열기 → 반영

1번을 하며 남긴 것:

1번을 하며 남긴 것:

- **내 이름·이메일 고치기가 아직 기기에만 저장된다.** 서버에 `PATCH /me` 가 없다.
  "우리" 탭 계정 칸에서 고쳐도 다시 로그인하면 서버 값으로 돌아간다
- **공간 이름을 여러 기기에서 동시에 고치면 나중에 저장한 쪽이 이긴다.**
  공간에는 여행처럼 `version` 이 없다. 관리자만 고칠 수 있어 부딪힐 일이 드물다고 봤다

### 메일 링크와 비밀번호 찾기 (2026-09-15)

확인 메일과 재설정 메일의 링크가 가리키는 페이지가 없어서, 가입은 되지만 이메일 확인과 비밀번호
재설정이 불가능했다. API 서버가 `/auth/verify-email`·`/auth/reset-password`·`/auth/forgot-password`
HTML 페이지를 직접 보여 주게 했다. GET은 아무것도 바꾸지 않고 버튼(POST)으로만 확인·변경한다.
앱 로그인 화면에 `비밀번호를 잊었어요`를 넣었다. 배포 스크립트가 nginx 설정 변경을 반영하지 않던
문제도 함께 고쳤다(내용이 바뀌면 nginx 컨테이너를 다시 만든다).

### 웹 재확인과 바뀐 이메일 (2026-09-16)

비밀번호 없이 소셜 로그인으로만 가입한 계정은 계정 삭제·비밀번호 정하기·이메일 바꾸기 앞에서
제공자로 다시 로그인해 확인받는다(`POST /v1/auth/oauth/reauth`). 이 길은 로그인과 같은
`openSocialLogin` 을 쓰므로 웹에서도 팝업으로 열리고 같은 출처의 `/oauth` 로 돌아온다.
팝업이 막히면(누른 직후가 아니거나 브라우저가 거절) `확인 창이 차단됐어요…` 를 화면에 적는다.
사용자가 창을 먼저 닫으면 취소로 보고 아무 문구도 띄우지 않는다.

이메일은 새 주소로 간 링크를 눌러야 바뀌어서, 열려 있는 앱은 그 사실을 모른다. 앱이 다시
앞으로 올 때(웹은 탭으로 돌아올 때)와 계정 화면을 열 때 `GET /v1/me` 를 다시 받아 화면과
저장된 세션을 맞춘다. 이메일 바꾸기를 요청한 직후 10분만 20초마다 한 번 더 본다. 바뀌면 멈춘다.
이름은 서버 값으로 덮지 않는다. 계정 화면에서 고치는 중이면 치던 글자가 되돌아가기 때문이다.
서버는 그대로 두고 앱만 고쳤다.

### 배포 정책 — 앱이 완성될 때까지

2026-09-15 웹 소셜 로그인도 연결했다. 서버가 켠 제공자 목록을 웹에 표시하고,
팝업은 같은 출처의 `/oauth`로 복귀한다. state·PKCE와 일회용 code 교환은 앱과 동일하다.
운영 서버 복귀 허용 목록에 `https://www.daymo.xyz/oauth`와 `https://daymo.xyz/oauth`를 추가했다.
`site/build.mjs`는 `/oauth`를 앱 HTML로 연결한다. 사이트 배포 시 앱과 이 설정을 함께 올려야 한다.
타입·린트·193개 테스트·웹 export와 모의 제공자를 이용한 브라우저 팝업/세션 저장 검증을 통과했다.
실제 Google·카카오·네이버 계정 로그인은 공개 웹 배포 후 소유자가 최종 확인한다.

- **서버는 main 푸시마다 자동 배포한다.** 앱이 운영 API에 붙어 있어서 서버가 뒤처지면 안 된다.
  DB migration도 이때 적용된다. 문서만 바꾼 커밋도 배포가 한 번 돌며 API가 잠깐 재시작된다
- **daymo.xyz 웹 자동 배포는 껐다**(`mobile/vercel.json` 의 `git.deploymentEnabled: false`).
  만들다 만 앱 화면을 공개 주소에 계속 올릴 이유가 없다. 약관·처리방침 페이지를 올릴 때 다시 켠다
- **EAS 빌드·스토어 제출·버전 올리기는 출시 후보가 생길 때까지 하지 않는다.** 2026-09-15 파이프라인
  확인용 Android `preview` 빌드를 한 번 돌려 원격 versionCode가 1이 됐다. 스토어에 올린 적이 없어
  그대로 두고, 첫 스토어 빌드는 2부터 시작한다. 앱 버전 `0.1.0` 과 iOS 빌드 번호는 건드리지 않았다

### 메일

2026-09-15 Resend로 `daymo.xyz` 발신 인증(SPF·DKIM, 도쿄 리전)을 마치고 운영 API 컨테이너가
키를 읽는 것을 확인했다. 운영 서버에서 Resend 시험 주소로 한 통 보내 SMTP 접수까지 확인했다.
`support@daymo.xyz` 수신은 Cloudflare Email Routing이다. DMARC 레코드(`_dmarc`)는 아직 없다.
**`runtime.env` 를 고친 뒤에는 api 컨테이너를 다시 만들어야 값이 들어간다.** `docker compose --env-file /etc/daymo/secrets/runtime.env -f /srv/daymo/current/backend/infra/production/compose.yml up -d --no-build api`. 파일만 고치고 이걸 빠뜨려서 한 번 키가 안 들어갔다.

### 앱스토어 심사 전에 남은 것

- **운영 반영은 끝났다(2026-09-15).** migration `38744d13f0c1` 까지 자동 배포됐고,
  VPS에 `daymo-cleanup.timer` 를 설치해 매일 04:40 KST에 돈다. 설치 직후 한 번 수동으로
  돌려 세 가지 정리가 모두 0건으로 끝나는 것을 확인했다
- **정기 백업이 돈다(2026-09-15).** 매일 04:00 KST에 DB dump와 사진 폴더를 restic으로
  암호화해 Google Drive에 올린다. 첫 snapshot을 받아 임시 DB에 복원까지 확인했고,
  매월 1일 같은 복원 검사가 자동으로 돈다. 실패하면 `support@daymo.xyz`로 메일이 온다.
  **restic password 사본을 VPS 밖에 보관해야 한다**(06 문서 8장)
- **Android 시험 빌드가 된다.** EAS `preview` 프로필로 APK가 나왔다(versionCode 1). 서명
  키는 EAS가 만들어 보관한다
- **개인정보 처리방침·이용약관 페이지.** 로그인 화면 아래 문구가 아직 링크가 아니다.
  처리방침에는 처리자 명칭·연락처·보호책임자가 들어가야 해서(08 문서 12장) 내용을
  정해야 만들 수 있다. 공개 저장소와 앱에는 실명을 넣지 않는다
- **관리자 넘기기.** 다른 멤버가 있는 공간의 관리자는 계정을 삭제할 수 없다. 넘기는
  API와 화면이 없어 지금은 막히기만 한다. 초대가 아직 없어 실제로 여럿이 쓰는 공간은
  없지만, 초대를 열 때 함께 만들어야 한다
- **삭제 원장.** 백업에서 복원하면 이미 정리한 계정이 되살아난다. 복원 뒤 다시 지우는
  deletion ledger가 없다
- **내 이름 바꾸기.** `PATCH /me` 가 없어 이름은 기기에만 바뀐다. 이메일 칸은 읽기
  전용으로 바꿨다
- **스토어 준비.** 심사용 데모 계정, iPhone 스크린샷(윈도우라 TestFlight 실기기로),
  App Privacy 답변. Expo 프로젝트는 `@hwangslater/daymo` 로 연결했고 `mobile/eas.json` 에
  `preview`(Android APK 내부 배포)·`production`(스토어, 빌드 번호 자동 증가) 프로필을 뒀다.
  아직 한 번도 빌드하지 않았다. iOS 빌드는 Apple Developer 가입 뒤에 된다

### 막혀 있는 것 — 바깥 자격증명이 있어야 한다

- **OAuth.** Google·Apple·Kakao·Naver 사업자 등록과 키가 필요하다. 키 없이도
  흐름(일회용 앱 로그인 코드 → 교환)은 만들 수 있다
- **bot challenge.** 검증 공급자를 문서가 일부러 미뤄 뒀다(처리 국가·SDK·비용
  재승인 필요). 지금은 시도 제한이 그 자리를 막고 있다
- **외부 백업.** VPS 로컬 DB 스냅샷과 복원 검사는 동작한다. Google Drive용
  restic·rclone 구성은 준비됐고 OAuth 연결 후 타이머를 켜고 실제 복원을 확인해야 한다

### 앱 쪽에 남은 것

- README 스크린샷은 2026-09-15에 새로 찍었다(비용 탭 포함 11장). 로컬 API에
  가명 데이터를 넣고 웹 빌드를 붙인 뒤, 헤드리스 Chrome을 426x880·2배로 열어
  찍었다. 백엔드에 CORS가 없어서 그 자동화 브라우저에서만 웹 보안 검사를 껐다.
  웹 렌더링이라 실기기와 글꼴·여백이 조금 다를 수 있다. 스토어용 스크린샷은
  실기기나 시뮬레이터로 따로 찍어야 한다

## 제품 원칙

- 앱을 열고 다음 여행을 확인하는 데 네트워크 때문에 오래 기다리지 않게 한다.
- 여행을 중심으로 일정, 장소, 준비, 요리, 기록을 구조화한다.
- 연인과 친구 공간은 같은 여행 기능을 쓰고 관계별 표현만 달리한다.
- 공동 편집에서 누가 담당하고 무엇이 완료됐는지 즉시 알 수 있어야 한다.
- 네이버·카카오 지도 링크는 지원하되 특정 제공자 링크를 필수 입력으로 만들지 않는다.
- 서버 연동 중에도 현재 UI의 빠른 반응을 유지하도록 낙관적 업데이트와 로컬 캐시를 사용한다.

## 우선순위 표기

- `P0`: 앱 출시와 데이터 안전에 필수
- `P1`: 현재 UI를 실제 기능으로 완성하는 범위
- `P2`: 출시 후 확장
- `결정 필요`: 구현 전에 사용자 승인을 받아야 하는 제품 또는 인프라 선택

## 현재 착수 판정

- 문서 설계: 개발 0단계 착수 가능
- 앱 기능 구현: [`10-implementation-readiness.md`](./10-implementation-readiness.md)의 P0 결정과 로컬 환경 게이트 완료 후 시작
- 운영 배포: 개인정보·외부 서비스·도메인·백업 결정 전까지 금지
