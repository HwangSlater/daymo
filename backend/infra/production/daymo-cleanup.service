[Unit]
Description=Purge Daymo accounts, trips and counters past their deadline
After=docker.service
Requires=docker.service
OnFailure=daymo-alert@%n.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/daymo-cleanup
